openwisp-controller~=0.8
# openwisp-utils[rest] is left unpinned so the version used is the one dictated by openwisp-controller
# Needed for the custom dashboard
# TODO: remove when the new version of openwisp-utils is released
# TODO: update below link before merging
openwisp-utils[rest] @ https://github.com/openwisp/openwisp-utils/tarball/dashboard
influxdb~=5.3
django-celery-email~=3.0.0
django-cache-memoize~=0.1
django-nested-admin~=3.3.2
swapper~=1.1
netaddr~=0.8
python-dateutil>=2.7,<3.0
